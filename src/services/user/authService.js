const authService = {
  login: async (credentials) => {},

  logout: async () => {
    return { data: null };
  },

  register: async (userData) => {},

  getUserInfo: async (userId) => {},
};

export default authService;
